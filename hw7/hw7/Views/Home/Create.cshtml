@model UserProfile

@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.EditorForModel()
    </div>
    <br>
    <div class="form-group">
        <input type="submit" class="btn btn-primary">
    </div> 
}